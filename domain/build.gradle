apply plugin: 'kotlin'

dependencies {
    api fileTree(dir: 'libs', include: ['*.jar'])

    api libs.kotlin
    api libs.timber

    api rx.values()
    api javax.values()

    //test
    testApi testLibs.values()
}
